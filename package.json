{
  "name": "site-link-explorer",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "TARGET_BROWSER=chrome vite",
    "dev:chrome": "TARGET_BROWSER=chrome vite",
    "dev:firefox": "TARGET_BROWSER=firefox vite",
    "build": "tsc && TARGET_BROWSER=chrome vite build",
    "build:chrome": "tsc && TARGET_BROWSER=chrome vite build",
    "build:firefox": "tsc && TARGET_BROWSER=firefox vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.1",
    "@vitejs/plugin-vue": "^5.1.0",
    "typescript": "~5.9.3",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.22"
  }
}
